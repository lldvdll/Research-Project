\wordcount{200}

\begin{itemize}
    \item Catastrophic forgetting in artificial networks has no clean analogue in biological learning, which motivates testing brain-inspired learning rules against it.
    \item We compare backpropagation, replay, and predictive coding (prospective configuration) on Split-MNIST under matched architecture, data, and stopping criteria.
    \item The comparison is controlled so that the learning rule is the only free variable, and every reported number is re-derived under the current protocol rather than carried over from earlier exploratory work.
\end{itemize}
