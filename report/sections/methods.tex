% ======================================================================================
% METHODS -- M1, M2, M3.  Budget ~1500 words.
%
% Figure slots are named for the 900 script that produces them. A slot with a \gap marker has
% no figure yet; the 900 script named in the comment is the one that will fill it.
% ======================================================================================

\section{Methods}
\label{sec:methods}

\subsection{Incremental Learning}
\label{sec:m0}

% This will include a diagram figure showing the difference between Domain and Class IL. 
% We will describe the two paradigms, how they differ, and why we are testing both.
Are we saying incremental of continual learning?
Be clear about the difference (if any) between the two.
Cite literature: van de Ven 2021 looks good. We may also include work from the Vincenzo Lomonaco course material, or other relevant references
Why do we care? What function does IL have? How does it relate to machine vs human learning?
What are the two scenarios (domain vs class IL), referencing the diagram.

\begin{figure*}[t]\centering
  \begin{panelbox}
    \solo{incremental_learning_diagram.png}
  \end{panelbox}
  \caption{\label{fig:il_diagram}Shows the difference between Domain IL and Class IL w.r.t. our setup.}
\end{figure*}

% --------------------------------------------------------------------- M1
\subsection{Catastrophic Forgetting}
\label{sec:m1}

% This section shows what forgetting looks like. For now, we use the figure we have. We may want to include a "what human forgetting looks like" figure later.
What does catastrophic forgetting look like? Describe figure 901.
Talk about the learning/forgetting tradeoff.
How does this differ from learning/forgetting in humans? Include a figure or some citations.

% Single-column float: 901 is one small panel, built at column width, and sits beside
% the text that reads it rather than spanning the page.
\begin{figure}[t]\centering
  \begin{panelbox}
    \solo{901_what_forgetting_looks_like.png}
  \end{panelbox}
  \caption{\label{fig:901}\wordcount{110}}
\end{figure}

\subsection{The learning rules: Back propagation vs Prospective Configuration}
\label{sec:m2}

% This section explains the core investigative question: Do local learning rules mitigate CF
Introduce core learning and "remembering" mechanisms in humans (memory, replay, meta-placticity, local learning)
Cite studies showing that replay etc are well studied.
Explain purpose of investigating local learning: Does it contribute to "remembering"?
Introduce Backprop as learning from global error signal, with reference to diagram
Introduce prospective configuration as learning from local error signals, with reference to diagram
Maybe comment on what we might expect to find based on theory
Maybe comment on what the literature says about the comparison, citing S&B results

\begin{figure*}[t]\centering
  \begin{panelbox}
    \solo{learning_rules_diagram.png}
  \end{panelbox}
  \caption{\label{fig:rules_diagram}\wordcount{110}}
\end{figure*}

% --------------------------------------------------------------------- M2
\subsection{Experiment setup and controls}
\label{sec:m3}

\wordcount{600}
% The parameter table carries the values; the prose carries only the JUSTIFICATION, and only
% where the justification is a measurement of ours rather than an inherited convention.
%   width          settled by 810/100 on capacity grounds, not inherited (prior work disagrees
%                  with itself about width). Note the trap: a capacity sweep at too short a
%                  budget produces a flat region indistinguishable from a ceiling.
%   activation     the one choice with no justification behind it until 802. Fig.~\ref{fig:903}.
%   output spec    120: the finding is insensitivity, not optimality.
%   settling       dt = 0.4 at H=32/depth 1, dt = 0.2 whenever width or depth varies. This is
%                  NOT a detail: 346/347 found dt = 0.4 oscillates at depth >= 2 and at
%                  Class-IL H = 4, sitting 3-7x above the true fixed point, and it cost two
%                  full sweep re-runs. Say that a control is re-verified whenever the
%                  configuration moves, not once at the start.

\begin{table}[t]
  \caption{\label{tab:params}Every control parameter, and what fixed it.\wordcount{60}}
  % \footnotesize and the shortened third column keep this inside \columnwidth. It overflowed
  % by 24.6pt at \normalsize -- check the log after adding a row.

  \footnotesize
  \begin{ruledtabular}
  \begin{tabular}{lll}
    Parameter & Value & Fixed by \\ \hline
    Input         & MNIST $14\times14$, $[0,1]$      & \gap{cite} \\
    Split         & $2\times5$, both scenarios       & \gap{cite} \\
    Hidden width  & $H=32$                           & 810 \\
    Hidden layers & 1                                & 342 \\
    Activation    & $\tanh$                          & 802 \\
    Output units  & 10 Class-IL, 5 Domain-IL         & structural \\
    Loss          & squared error                    & 812 \\
    Optimiser     & SGD, batch 32                    & \gap{cite} \\
    Learning rate & \gap{0.02, both rules}           & 340, 314 \\
    PC settling   & $dt=0.4$; $0.2$ off-point        & 334, 347 \\
    Stopping      & 90\% on both tasks               & \cref{sec:m3} \\
    Seeds         & 10--19, paired                   & 906 \\
  \end{tabular}
  \end{ruledtabular}
\end{table}



% Input row of the table: what 14x14 costs the image.
\begin{figure*}[t]\centering
  \begin{panelbox}
    \solo{910_input_resampling.png}
  \end{panelbox}
  \caption{\label{fig:910}\wordcount{60}}
\end{figure*}

% Hidden-width row, and the prior question of whether the trunk does any work at all.
\begin{figure*}[t]\centering
  \begin{panelbox}
    \panel{0.49\panelspan}{902_architecture_justification_a.png}\hfill
    \panel{0.49\panelspan}{902_architecture_justification_b.png}
  \end{panelbox}
  \caption{\label{fig:902}\wordcount{130}}
\end{figure*}

\begin{figure*}[t]\centering
  \begin{panelbox}
    \panel{0.98\panelspan}{903_specification_a.png}\par\vspace{6pt}
    \panel{0.98\panelspan}{903_specification_b.png}
  \end{panelbox}
  \caption{\label{fig:903}\gap{903 not written; needs 802, which is queued}\wordcount{110}}
\end{figure*}

\begin{figure*}[t]\centering
  \begin{panelbox}
    \solo{904_settling_fixed_point.png}
  \end{panelbox}
  \caption{\label{fig:904}\gap{904 not written; copies script 334}\wordcount{110}}
\end{figure*}

% Seeds row: what pairing buys, as the licence for every paired statistic downstream.
\begin{figure*}[t]\centering
  \begin{panelbox}
    \solo{906_what_pairing_buys.png}
  \end{panelbox}
  \caption{\label{fig:906}\wordcount{90}}
\end{figure*}

% --------------------------------------------------------------------- M3
\subsection{Measuring forgetting}
\label{sec:m4}

\wordcount{450}
% This is a control presented as a result, because it licenses every comparison downstream and
% because a reader will otherwise assume the metric was chosen to produce the answer.
%
% ⚠ THE ARGUMENT MUST BE MADE FROM SINGLE-RULE FAILURES ONLY. Justify the metric from 813
% (endpoint metrics drift with the stopping threshold) and 814 (crossover is censored at high
% lr), both of which are properties of ONE rule measured against itself. Do NOT justify it by
% 815/112, which selects the metric by which one preserves the PC-backprop sign -- that is
% choosing the instrument by the answer it gives on the comparison it will then be used for.
% 815 belongs in the appendix as a robustness check AFTER the result, and is cited that way.
%
% Then state plainly what crossover cannot do: its RAW value is not stopping-point independent
% (110: Domain-IL 36.2 -> 75.3 across a budget sweep). The stable quantity is the PAIRED
% difference, which is what every Results figure plots.
%
% And state that a censored crossover is the BEST outcome, not a missing one -- task 1 never
% fell below task 2 -- so it is ranked, never dropped.

\begin{figure*}[t]\centering
  \begin{panelbox}
    \panel{0.98\panelspan}{907_why_matched_competence_a.png}\par\vspace{6pt}
    \panel{0.98\panelspan}{907_why_matched_competence_b.png}
  \end{panelbox}
  \caption{\label{fig:907}\gap{907 not written; merges 111 and 343}\wordcount{110}}
\end{figure*}

\begin{figure*}[t]\centering
  \begin{panelbox}
    \solo{909_crossover_vs_retention.png}
  \end{panelbox}
  \caption{\label{fig:909}\wordcount{110}}
\end{figure*}
